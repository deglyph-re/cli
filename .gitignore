.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/
.DS_Store
*.sarif
.claude
TODO.md

# Private launch / business setup notes; never publish.
LAUNCH.md

# Binaries under analysis must never be committed (they are large and often
# proprietary). The one exception is the committed CI fixture.
*.exe
*.dll
*.so
*.dylib
*.bin
!samples/demo.exe

# Regression fixtures are built from samples/fixture_src.c by
# samples/build_fixtures.sh (and in CI), never committed. They have no extension,
# so ignore them by name; the build script and source stay tracked.
samples/fixture_macho_*
samples/fixture_elf_*
samples/fixture_pe_*
